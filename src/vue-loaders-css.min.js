import VueLoadersCss from './VueLoadersCss.vue'

function install(Vue) {
  Vue.component('vue-loaders-css', VueLoadersCss)
}

export default {
  install
}

if (typeof window !== 'undefined' && window.Vue) {
  Vue.use( {
    install
  })
}
